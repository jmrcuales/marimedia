# Homepage photography

Approved assets for homepage section slots only.

Populate `homepageContent.*.image` in `lib/content/homepage.ts` only after an
asset here has a completed `ASSET-MANIFEST.md` entry and human approval.

Current status: empty on purpose (MARIWEB-010).
